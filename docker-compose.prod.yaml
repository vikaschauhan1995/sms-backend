services:
  backend:
    build:
      context: '.'
      dockerfile: ./Dockerfile.prod
    container_name: sms-frontend-prod-container
    networks:
      - sms-app-prod-network
    volumes:
      - ./:/app
    env_file:
      - C:/Users/91987/Desktop/personal/SMS/SECRET/.env
    environment:
      - BACKEND_PORT=${BACKEND_PORT}
    ports:
      - ${BACKEND_PORT}:${BACKEND_PORT}
networks:
  sms-app-prod-network:
